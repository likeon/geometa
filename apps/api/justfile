drizzle-generate:
  bunx drizzle-kit generate

drizzle-migrate:
  bunx drizzle-kit migrate

db-psql:
  psql "{{ env_var_or_default('DATABASE_URL', 'postgresql://postgres:postgres@localhost:5432/geometa') }}"

alias db-shell := db-psql

environment := "local"
db-dump:
  ./utils/db/backup.sh {{environment}}

db-reset:
  # abort if template database (geometa_backup) used by reset.sql does not exist
  psql "{{ env_var_or_default('DATABASE_URL_POSTGRES_DB', 'postgresql://postgres:postgres@localhost:5432/postgres') }}" -tAc "SELECT 1 FROM pg_database WHERE datname = 'geometa_backup'" | grep -q 1 || (echo "Error: template database 'geometa_backup' does not exist, run 'just db-dump' first" && exit 1)
  psql "{{ env_var_or_default('DATABASE_URL_POSTGRES_DB', 'postgresql://postgres:postgres@localhost:5432/postgres') }}" -f utils/db/reset.sql

db-init:
  psql "{{ env_var_or_default('DATABASE_URL', 'postgresql://postgres:postgres@localhost:5432/geometa') }}" -f utils/db/init.sql

build TAG:
  buildah bud --platform linux/arm64 -t {{TAG}}
